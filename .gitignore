# macOS
.DS_Store
._*

# Arquivos temporários do Office
~$*

# Material de origem — fica no disco local, não sobe para o repositório.
# As versões otimizadas (assets/img, assets/fonts, assets/marca) é que são versionadas.
assets/Fonte/
assets/Fotos_originais/
assets/Texto_original/
assets/ID_visual/

# Ferramental de agente — não tem relação com o site e, como o Pages serve a
# raiz do repositório, subir isto publicaria os scripts junto.
.agents/

# Logo da SBCP: material de origem, não referenciado por nenhuma página.
# Se um dia entrar no site, tirar daqui e versionar a versão otimizada.
assets/SBCP-logo.png

# Ferramentas
node_modules/
